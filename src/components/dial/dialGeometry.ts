// Shared SVG coordinate constants for the dial.
// Kept in a separate file so child components can import them
// without creating a circular dependency with Dial.tsx.
export const CX = 400           // pivot x (viewBox units)
export const CY = 420           // pivot y (viewBox units)
export const R = 360            // semicircle radius
export const NEEDLE_LENGTH = 320
